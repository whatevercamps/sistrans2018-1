CREATE TABLE OPERADORES (
ID NUMBER GENERATED by default on null as IDENTITY,
NOMBRE VARCHAR2(60 BYTE) NOT NULL, 
TIPO NUMBER NOT NULL, 
CONSTRAINT PK_OERADOR PRIMARY KEY (ID)
);

ALTER TABLE OPERADORES ADD(
CAPACIDAD NUMBER NOT NULL,
MIN_TIEMPO_DIAS NUMBER NOT NULL,
CONSTRAINT CK_CAPACIDAD CHECK (CAPACIDAD >= 0),
CONSTRAINT CK_MIN_OPERA CHECK (MIN_TIEMPO_DIAS >= 0));

COMMIT;